---
interface Props {
  instructions: string[];
  title?: string;
  id?: string;
}
const { instructions, title = 'Register State Machine' } = Astro.props;
const payload = JSON.stringify({ instructions, title });
---

<div data-weapon-component="assembly-visualizer" data-weapon-props={payload}></div>
